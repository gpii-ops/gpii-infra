# NOTE! This file is generated!

apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: preferences
spec:
  replicas: 2
  template:
    metadata:
      labels:
        app: preferences
    spec:
      containers:
      - name: preferences
        image: <%= @versions["preferences"] %>
        ports:
        - containerPort: 8081
        env:
        - name: NODE_ENV
          value: gpii.config.preferencesServer.standalone.production
        - name: GPII_PREFERENCES_DATASOURCE_URL
          value: http://<%= @secrets[:couchdb_user] %>:<%= @secrets[:couchdb_password] %>@couchdb.default.svc.cluster.local:5984/preferences/%userToken
        - name: GPII_PREFERENCES_LISTEN_PORT
          value: '8081'
