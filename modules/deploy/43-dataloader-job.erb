# NOTE! This file is generated!

apiVersion: batch/v1
kind: Job
metadata:
  name: couchdb-dataloader
spec:
  template:
    metadata:
      labels:
        app: couchdb-dataloader
    spec:
      containers:
      - name: couchdb-dataloader
        image: <%= @versions["preferences-dataloader"] %>
        env:
        - name: COUCHDB_URL
          value: http://<%= @devsecrets["couchdb_dev_user"] %>:<%= @devsecrets["couchdb_dev_password"] %>@couchdb.default.svc.cluster.local:5984/preferences
        - name: CLEAR_INDEX
          value: 'true'
      restartPolicy: OnFailure
