# NOTE! This file is generated!

apiVersion: v1
kind: Service
metadata:
  name: preferences
  labels:
    app: preferences
  annotations:
    dns.alpha.kubernetes.io/external: "preferences.<%= ENV["TF_VAR_cluster_name"] %>"
spec:
  selector:
    app: preferences
  ports:
  - port: 80
    targetPort: 8081
  type: LoadBalancer # exposed externally
