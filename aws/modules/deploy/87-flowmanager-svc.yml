apiVersion: v1
kind: Service
metadata:
  namespace: gpii
  name: flowmanager
spec:
  selector:
    app: flowmanager
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8081
  type: ClusterIP