apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: flowmanager-cert
  namespace: gpii
spec:
  secretName: flowmanager-tls
  issuerRef:
    name: letsencrypt
  dnsNames:
  - flowmanager.<%= ENV["TF_VAR_environment"] %>.gpii.net
  acme:
    config:
    - http01:
        ingressClass: nginx
      domains:
      - flowmanager.<%= ENV["TF_VAR_environment"] %>.gpii.net
