{{- if .Values.enableStackdriverTrace }}
apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry
metadata:
  namespace: {{ .Release.Namespace | quote }}
  name: {{ template "flowmanager.name" . }}-newrelic
spec:
  hosts:
  - "*.newrelic.com"
  ports:
  - number: 443
    name: tls
    protocol: TLS
  ###resolution: DNS
  ###location: MESH_EXTERNAL
{{- end }}
