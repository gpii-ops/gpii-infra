apiVersion: authentication.istio.io/v1alpha1
kind: Policy
metadata:
  name: {{ template "preferences.name" . }}-permissive
spec:
  targets:
  - name: {{ template "preferences.name" . }}
  peers:
  - mtls:
      mode: PERMISSIVE
