apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: "{{ template "preferences.name" . }}-tls-disable"
spec:
  host: "{{ template "preferences.name" . }}.{{ .Release.Namespace }}.svc.cluster.local"
  trafficPolicy:
    tls:
      mode: DISABLE
