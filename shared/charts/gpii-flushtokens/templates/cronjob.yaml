apiVersion: batch/v1beta1
kind: CronJob
metadata:
  namespace: {{ .Release.Namespace | quote }}
  name: {{ template "flushtokens.name" . }}
spec:
  schedule: "*/15 * * * *"
  concurrencyPolicy: Allow
  startingDeadlineSeconds: 200
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: {{ template "flushtokens.name" . }}
        spec:
          containers:
          - name: gpii-flushtokens
            image: "{{ .Values.image.repository }}@{{ .Values.image.checksum }}"
            command: [ "/bin/sh", "-c", "/app/scripts/dockerDeleteExpiredAccessTokens.sh; curl -s -XPOST http://127.0.0.1:15000/quitquitquit" ]
            env:
            - name: GPII_COUCHDB_URL
              value: '{{ .Values.couchdbUrl }}'
            # Use '--deleteAll' to delete all of the access tokens
            - name: DELETE_ALL
              value: 'false'
          restartPolicy: OnFailure
