apiVersion: v1
kind: Service
metadata:
  name: preferences
  labels:
    app: preferences
  annotations:
    dns.alpha.kubernetes.io/external: "preferences.k8s-dev-tyler.gpii.net"
spec:
  selector:
    app: preferences
  ports:
  - port: 80
    targetPort: 8081
  type: LoadBalancer # exposed externally
