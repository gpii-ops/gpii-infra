apiVersion: v1
kind: Service
metadata:
  name: flow-manager
  annotations:
    dns.alpha.kubernetes.io/external: "flow-manager.<%= ENV["TF_VAR_cluster_name"] %>"
spec:
  selector:
    app: flow-manager
  ports:
  - port: 80
    targetPort: 8081
  type: LoadBalancer # exposed externally
